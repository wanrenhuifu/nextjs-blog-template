| 属性 | 值 |
|------|-----|
| version | v2.0 |
| name | 站点名 —— 取自根目录 `site.config.mjs`，模板默认为「示例博客」 |
| description | 双主题个人博客系统。日间「宣纸墨字」—— 近白纸底配**朱红**主色，Hero 是以内联 SVG 绘制的墨竹，分远近三层做 **CSS 3D 纵深**；夜间「墨夜朱印」—— 近黑夜空配提亮朱红，Hero 呈现金色月轮与多层光晕，月与星野置于最远层。标题使用中文衬线体（宋体系系统栈），正文使用系统无衬线体。交互上包含 Hero 的 3D 视角跟随（鼠标）与滚动视差、stagger 入场动画、卡片悬停浮起、双主题统一瞬时切换（next-themes 在切换瞬间禁用全站过渡避免闪烁）。主题默认按访客本地时间自动切换日/夜（06:00–17:59 日间、18:00–05:59 夜间，边界常量见 `lib/timeTheme.ts`），首次进入即按当前时刻落地对应主题；用户手动点按切换按钮后锁定所选主题、退出自动跟随（不提供「回到自动」的 UI，清除站点数据可恢复）。 |

> **v2.0 变更**：配色整体重做（旧版为淡竹青 + 米白，实测日间对比度过低、观感发灰）。同时解除了两类旧约束——不再禁止纯黑/纯白与直接书写 hex，新增色值也不必先登记本文档。3D 相关的实现约束见文末「Hero 背景系统」。

---

## 核心设计理念：宣纸墨字，墨夜朱印

**日间（宣纸墨字）**：以近白纸底托住高对比的墨色文字，朱红作为唯一的强色出现在标签、链接、按钮等需要被看见的地方。竹以墨色分浓淡写就——近景浓墨、中景淡墨、远景几近于无，靠墨色浓淡而非堆叠数量拉出纵深。

**夜间（墨夜朱印）**：近黑夜空为底，暖金星辉是核心视觉焦点。蓝色氛围全部交给夜空辉光（`night-glow`）营造，底色本身保持中性，避免底色与辉光互相抵消。正文用冷灰蓝，标题用暖白。

**两版的共同取舍**：把对比度当作第一约束。旧版日间底色 `#FCFDF9` 与卡片 `#F2F7F1` 明度差不到 5%，小字号 `muted` 只有约 4.0:1，低于 4.5:1 门槛——这是「发灰」的根因。v2.0 所有正文级文本在各自的底色上都稳过 AA。

---

## Design Tokens

### Colors — Surface & Text

| Token | 日间 | 夜间 | 用途 |
|-------|--------------|--------------|------|
| `app` | #FAFAF8 | #0A0A0D | 页面底色 |
| `card` | #FFFFFF | #151519 | 卡片背景。**日间比底色亮**（旧版做到更暗，把「抬起」做成了「凹陷」） |
| `hover` | #F0F0EC | #202027 | 悬停表面 |
| `borderline` | #E5E5DF | #2C2C34 | 边框与分割线 |
| `title` | #0A0A0B | #F5F5F2 | 主标题与强调文本（日间 18.9:1） |
| `body` | #2E2E33 | #C6C6CE | 正文（日间 12.9:1） |
| `muted` | #6E6E76 | #8E8E9A | 次要文本、日期、标签（日间 4.8:1，均过 AA） |
| `on-primary` | #FFFFFF | #0A0A0D | 主色背景上的文本 |

### Colors — Brand & Accent

一主一辅：**朱红**承担所有「需要被看见」的地方，**青**用于链接与数据类信息，与朱红形成冷暖对位。

| Token | 日间 | 夜间 | 用途 |
|-------|--------------|--------------|------|
| `primary` | #C43A1B | #FF6B4A | 朱红 — 主品牌色（日间对小字 4.98:1） |
| `primary-strong` | #A32E14 | #FF6B4A | 主色文字级变体（日间 6.7:1，用于 Hero 导航等小字号） |
| `primary-hover` | #A93318 | #FF8A6E | 悬停。**方向相反**：日间变深（保证白字仍 6.6:1），夜间变亮 |
| `primary-active` | #8C2913 | #E85535 | 激活/按下 |
| `accent` | #FBEDE8 | #1E1E24 | 朱红淡染 — 点缀表面 |
| `decor` | #8A8A92 | #6A6A76 | 中性灰 — 装饰与次要图标 |
| `pop` | transparent | #FF6B4A | 弹出强调 |
| `accent-2` | #B26A00 | #F0B429 | 第二强调（暖琥珀） |
| `accent-3` | #0B6E77 | #5AA9E6 | 第三强调（青/星蓝） |

### Colors — Status & Link

功能色全部避开了与朱红主色的色相撞车：`danger` 取偏红的 #BE1E3C（色相 348°）而主色在 14°，并排时能区分开。

| Token | 日间 | 夜间 | 用途 |
|-------|--------------|--------------|------|
| `success` | #2F7D4F | #3FBF7F | 成功 |
| `warning` | #A16207 | #F0B429 | 警告 |
| `danger` | #BE1E3C | #FF6B81 | 错误 |
| `info` | #1D6FB8 | #5AA9E6 | 信息 |
| `caution` | #8A6D0B | #E0B93C | 警示/注意 |
| `link` | #0B6E77 | #3FC6D2 | 链接文本 |
| `link-hover` | #095256 | #6FDCE6 | 链接悬停 |
| `link-visited` | #7A3E9D | #B79CE8 | 已访问链接 |
| `code-bg` | #101012 | #08080A | 代码块背景（两侧均为深色） |
| `code-line-highlight` | #1C1C20 | #1A1A20 | 代码块行高亮 |

### Gray Scale (中性灰阶)

日间为暖中性灰，夜间为冷中性灰。**夜间 900 最浅、日间 900 最深**——两端反转使同一 `text-gray-900` 在两个主题下均可读，但直接比对数值会感到反直觉。

| Token | 日间 | 夜间 |
|-------|--------------|--------------|
| `gray-50` | #FAFAF8 | #0A0A0D |
| `gray-100` | #F4F4F1 | #151519 |
| `gray-200` | #E8E8E4 | #202027 |
| `gray-300` | #D6D6D0 | #2C2C34 |
| `gray-400` | #A8A8A2 | #3E3E48 |
| `gray-500` | #7C7C78 | #5A5A66 |
| `gray-600` | #5C5C5A | #8E8E9A |
| `gray-700` | #40403E | #C6C6CE |
| `gray-800` | #262625 | #E0E0E6 |
| `gray-900` | #0F0F10 | #F5F5F2 |

### Elevation (卡片高度)

日间靠投影把卡片从纸底上托起来；夜间投影几乎不可见，层次改由表面色阶（`app` → `card` → `hover`）与 1px 边框承担。

| Token | 日间 | 夜间 |
|-------|------|------|
| `shadow-card` | `0 1px 2px rgba(10,10,11,.04), 0 1px 3px rgba(10,10,11,.06)` | `0 1px 2px rgba(0,0,0,.40)` |
| `shadow-lift` | `0 4px 12px rgba(10,10,11,.06), 0 12px 32px -8px rgba(10,10,11,.10)` | `0 8px 24px -6px rgba(0,0,0,.60)` |

### Special Surfaces

- **毛玻璃 Glass BG** 日间 `rgba(250,250,248,0.80)` / 夜间 `rgba(10,10,13,0.85)`
- **遮罩层 Overlay** 日间 `rgba(10,10,11,0.40)` / 夜间 `rgba(0,0,0,0.80)`
- **文本选区 Selection** 日间 `rgba(196,58,27,0.22)`（朱红调）/ 夜间 `rgba(255,107,74,0.25)`
- **骨架屏 Skeleton** 日间 `#F4F4F1 → #E8E8E4` / 夜间 `#151519 → #2C2C34`
- **页头高度** `--header-h: 52px`，定义在 `theme.css` 的 `@layer base`。页头自身高度、吸顶标题的 `top`、`scroll-padding-top` 全部由它派生——改这一个值即可整体联动。

### Cursor（光标）

光标延续双主题意象，通过 `styles/theme.css` 中的 `--cursor-default` / `--cursor-pointer` 全局生效。

| 主题 | 形状 | 热点 | 配色 | 说明 |
|------|------|------|------|------|
| 日间 | 竹叶指针 | 叶尖 (2,2) | 墨色渐变 `#8A928B → #4A544C → #232A25`，描边 `#14171A` | 渲染 32×32（viewBox 48），rotate(-45°) 叶尖指向左上，含叶脉与白色衬边，任何背景下清晰可辨 |
| 夜间 | 五角星 | 左上角尖 (4,6) | 星辉金渐变 `#FFE082 → #FFC93C → #F9A825`，描边 `#B26A00` | 渲染 40×40（viewBox 64）。金色与月轮同源，不改朱红——星星属于场景，不属于品牌色。**与日间一样必须带白色衬边**：`stroke-width` 需明显大于描边的 2.4，才能在外侧留出一圈可见白边。缺了它，金星压在夜晚的朱红按钮上会糊掉 —— 金色与朱红色相只差约 30°，而深褐描边在红底上只是一圈脏边 |

**行为规则**：自定义光标只用于非文字区域——可选中文字（prose 正文、标题、表格、表单输入框、`[contenteditable]`）恢复系统 I 型光标；链接/按钮等交互元素的后代继承父级手型。规则集中在 `styles/theme.css` 的 `@layer base` 光标段。

### Hero 场景色板（Scene Palette）

场景色值**不随主题翻转**——日间场景只在日间显示、夜间场景只在夜间显示，因此双主题块写同值。需要透明变体时在 `hero-scene.css` 里用 `color-mix(in srgb, var(--theme-*) N%, transparent)` 混出。

竹的墨色**分三级**（`TONE` 常量在 `HeroSection.tsx`），浓淡差是纵深的主要来源：

| Token | 色值（日夜同值） | 用途 |
|-------|----------------|------|
| `stalk-near` | #232A25 | 近景竹竿 — 浓墨，渐变峰值浓度 0.78 |
| `stalk-far` | #6E766F | 中景与远景竹竿 — 淡墨，浓度 0.32 / 0.16 |
| `leaf` | #3A453C | 近景竹叶 |
| `dust` | #8F968F | 浮尘微粒 |
| `dapple-warm` | #F0E7D0 | 日间斑驳暖光斑基色 |
| `night-glow` | #3F618F | 夜空顶部环境辉光基色 |
| `moon-hi` | #F4DCA6 | 月轮高光（渐变 0%） |
| `moon-mid` | #E7C47D | 月轮中间调（渐变 42%） |
| `moon-core` | #D4A853 | 月轮主色（渐变 68%；光晕/光环亦以此为基色） |
| `moon-edge` | #B08A44 | 月轮边缘暗调（渐变 100%） |
| `moon-spot` | #856737 | 月面浅斑基色 |
| `star` | #E9E5D8 | 星点（月白，大星野） |
| `star-gold` | #E3C98A | 星点（淡金，小星野，与月轮呼应） |

---

## Typography

排版系统以 CSS 自定义属性与 Tailwind `@theme` 标记共同定义。标题使用中文衬线体栈（宋体系系统字体），正文使用系统无衬线体栈，代码使用 JetBrains Mono。

> **字体加载：全部走系统字体，不加载任何网络字体。**
>
> 曾用 `next/font/google` 自托管 Noto Serif SC 并配 `display: "optional"`。移除的原因是实测数据：思源宋体按 `unicode-range` 切成约 120 个子集，首页的中文正好跨到 10 个，合计 **745KB**，占整页 1061KB 的 **70%**；而 `display: "optional"` 意味着慢速网络下这批字体等不到、首屏永远用回退栈渲染——**下载了却不会被使用**，纯粹占用带宽（Slow 4G 下 3.7 秒）拖累 CSS/JS 到达。实测拦截字体请求后首页 LCP 从 5516ms 降到 1856ms。移除后实测：**LCP 5516ms → 1684ms，传输 1075KB → 264KB**，并连带消失一支装着 102 条 `@font-face`、gzip 33KB 且阻塞渲染的 CSS。
>
> 敢直接删的底气来自回退栈本来就按「与 Noto 同风格」挑选：全栈统一为宋体系（Songti SC / Source Han Serif SC / Noto Serif CJK SC / SimSun），历史上移除回退链末位的楷体（笔刷风格）就是为了避免风格跳变。大字号下系统宋体的观感与思源宋体基本无差，笔画甚至更锐利。
>
> **约束**：标题字体必须走 `--font-serif`（宋体系），不得引入楷体等异风格字体，也不得再引入 CJK 网络字体。`scripts/generate-og.mjs` 的 SVG 字体栈必须与 `--font-serif` 一致，且不得出现 `KaiTi`。（图标已改为纯 path 绘制、不再用 `<text>`，故不受此约束。）
>
> **注意 `font-family` 与 CSS 变量的组合写法**：`font-family: var(--some-undefined-var), serif` 中只要有一个 `var()` 未定义，整条声明会在计算值阶段失效，元素静默退回继承字体（往往是无衬线正文体）。历史上 `PageTitle.tsx` 等处的内联样式就这样引用过被删掉的 `--font-noto-serif-sc`，删字体时必须一并清理这类引用。

| Token | fontFamily | fontSize | fontWeight | lineHeight | letterSpacing |
|-------|-----------|----------|------------|------------|---------------|
| display-xl | Songti SC, Source Han Serif SC, Noto Serif CJK SC, SimSun, serif | clamp(2.25rem, 5vw, 4.5rem) | 700 | 1.4 | 0.02em |
| title-md | 同上（`--font-serif`） | 1.25rem | 700 | 1.4 | 0 |
| title-sm | 同上（`--font-serif`） | 1.125rem | 600 | 1.35 | 0 |
| body-md | system-ui, -apple-system, sans-serif | 1rem | 400 | 1.65 | 0 |
| body-sm | system-ui, -apple-system, sans-serif | 0.875rem | 400 | 1.6 | 0 |
| caption | system-ui, -apple-system, sans-serif | 0.75rem | 400 | 1.5 | 0 |
| code | JetBrains Mono, ui-monospace, monospace | 0.875rem | 400 | 1.7 | 0 |

### Spacing & Rounded

间距沿用 Tailwind v4 默认 4px 基准（`p-1` = 4px, `p-4` = 16px）。圆角使用 Tailwind v4 的默认档位：

| Token | 值 | 典型用途 |
|-------|-----|---------|
| `rounded-sm` | 4px | 极小元素 |
| `rounded-md` | 6px | 标签、徽标 |
| `rounded-lg` | 8px | 小控件 |
| `rounded-xl` | 12px | 按钮、输入框 |
| `rounded-2xl` | 16px | 卡片、模态框、代码块 |
| `rounded-full` | 9999px | 头像、药丸按钮 |

> **历史勘误**：v1.0 的本文档标注为 6/8/12/12/16px，那是 Tailwind v3 的数值，而项目跑 v4，实际生效的是上表。旧文档据此在组件里写了不少 `rounded-[10px]`、`rounded-[14px]` 之类的游离值以「对齐文档」，属误伤。

---

## 页面结构

### 首页

刻意保持「Hero + 纯文字目录」两段式：

1. **Hero** —— 一屏 3D 场景，详见下节。
2. **文章目录** —— `max-w-4xl` 的纯排版列表，每行 `日期 · 分类 · 标题`，等宽字体排日期以便对齐，下附一行纯文字入口（文章 / 工坊 / 关于）。

**不使用卡片。** 卡片会让首页从「一屏场景 + 一份目录」膨胀成三段同构的栏目块（旧版正是如此：三个无衬线的两行大标题 + 六张灰卡片，读起来像模板）。

**不使用 FadeUp。** 入场动画依赖 `IntersectionObserver` 与 `opacity: 0` 初始态，JS 失败时内容会永久不可见。首页正文是站点最重要的内容，不做这种依赖；动效留给 Hero。

### 其他页面

列表页（`/blog`、`/tags/[tag]`）用三列卡片栅格，`/types` 与 `/archive` 用密集单行列表配吸顶分组标题，文章详情页为正文加桌面端右侧目录。这些页面的形态未变。

---

## 性能

静态导出（`output: "export"`）+ GitHub Pages，无后端、无运行时数据获取。性能上唯一需要盯的是**首屏传输量与关键路径**。

**测量工具**（复用 `playwright-chromium`，无需额外依赖）：

| 脚本 | 用途 |
|------|------|
| `node scripts/audit/perf-audit.mjs` | FCP / LCP / CLS + 分类型传输量，正常网速与 Slow 4G（1.6Mbps / 150ms RTT）两档。`--no-fonts` 可拦截字体做对照 |
| `node scripts/audit/perf-assets.mjs <路径>` | 列出该页全部资源按体积排序，并报出 LCP 元素是谁 |

**只用 `out/` 产物测**——`next dev` 未压缩未优化，数字没有参考价值。

**当前基线**（首页，Slow 4G）：FCP 948ms / LCP 1684ms / CLS 0 / 传输 264KB。

**已确立的取舍**：

- **不加载 CJK 网络字体**。理由与实测数据见上文 Typography 节。这是本项目单笔收益最大的性能决策（-811KB，LCP 3.3×）。
- **Hero 全部为内联 SVG**。首页 HTML 未压缩 117KB，但 gzip 后仅 20.4KB——内联矢量标记的压缩比极高，不构成传输问题；代价是 910 个 DOM 节点与其渲染成本，这个量级仍在健康范围。
- **首屏不依赖 JS**。Hero 场景、文章目录、页脚都是 SSR 输出的静态标记；`prefers-reduced-motion` 与 JS 失败时内容照常可见。

**已知待办**：

- `@tailwindcss/typography` 全量输出 **564 条 `prose` 规则**，占主 CSS 文件（132KB raw / 21.1KB gzip）的大部分。可精简，但会触碰文章排版，需逐条核对。
- 顶部导航的 `<Link>` 触发 **31 个 RSC 预取请求**（合计 11.5KB）。总量很小，加 `prefetch={false}` 可去掉这批噪声请求。

**正文图片的懒加载与尺寸**

文章内图片一律带 `loading="lazy"` `decoding="async"` 与原始宽高。实现分两条路径（原因见 `lib/AGENTS.md` 第 9 条）：Markdown 图片走 `MdxContent` 的 `img` 覆盖，手写 `<img>` 走 `content.ts` 的 `enhanceRawImages`。

**关键认知：`loading="lazy"` 的效果与连接质量强相关。** Chrome 的懒加载距离阈值随有效连接类型收缩（快网约 3000px、慢网约 1250px）。同一张位于文档 2745px 处的图：

| | 快网 | Slow 4G |
|---|---|---|
| 初始加载是否拉取该图 | 是（2745px < 3000px 阈值） | 否（2745px > 1250px 阈值） |
| 该页 `load` | — | **5364ms → 2254ms** |
| 该页传输 | — | **878.5KB → 393.2KB** |

也就是说：带宽充裕时浏览器提前备好图，带宽紧张时绝不占用关键路径 —— 正是期望的行为。**验证这类改动必须在限速档下测**：不限速时 Chrome 会照常预取，看起来「懒加载没生效」，容易误判。

关于宽高：它的作用是让浏览器在图片到达前按原始比例占好位。本项目中该图的布局增长恰好发生在视口之外（浏览器提前拉取所致），所以 CLS 前后都是 0.0000，**未能复现可测量的收益**；保留它是防御性的——连接更慢或阈值收缩时，增长可能正好发生在视口内。

**但懒加载只是缓解，压缩才是根治。** 上面那张位于 2745px 处的演示图原始体积 **485KB**（1080×1085）。懒加载把它从慢速网络的关键路径上摘掉了，可一旦读者滚到那里，仍要等这 485KB 下载完；按 WebP q≈78 重新编码可望降到 100–150KB 量级。模板化时这张图已替换为 2.7KB 的占位图，但规律对你自己加的配图依然成立：**提交配图前先压缩**（`public/AGENTS.md` 建议单张 < 500KB，实际应远低于此）。

---

## Hero 背景系统

场景为**内联 SVG 绘制 + CSS 3D 纵深**。DOM 与绘制在 `components/home/HeroScenery.tsx`，构图与时序在 `components/home/HeroSection.tsx`，样式在 `styles/hero-scene.css`。两套场景（`.scene-day` / `.scene-night`）常驻 DOM、`pointer-events: none`，交叉淡出由 Framer Motion 驱动（0.9s、reduced-motion 时瞬时）。

### 画面分层（由远及近）

**日间「墨竹山水」** —— 柔光斑 → 淡墨远山 → 云气 → 远景竹 → 中景竹 → 云气 → 近景竹 → 光柱 → 浮尘 → 飘落竹叶 → 纸纹/渐晕

**夜间「星月夜」** —— 夜空辉光 → 星河 → 月轮与光环 + 星野 → 月光远山 → 云气 → 竹影 → 萤火 → 纸纹/渐晕

### 3D 层级结构

```
.scene              绝对定位铺满 + overflow:hidden + opacity（Framer 驱动日夜淡出）
└ .hero-perspective   perspective: 1400px，perspective-origin: 50% 42%
  └ .hero-space       preserve-3d，承载鼠标驱动的 rotateX / rotateY
    └ .hero-layer     静态 translateZ，决定纵深
      └ [motion]      滚动视差 translateY（Framer 内联，不覆盖上面的 Z）
```

纵深取值：`far` −240px / `mid` −110px / `near` 0 / `deep` −300px。

### ⚠ 四个已知陷阱（都踩过，改 3D 前必读）

1. **`opacity < 1`、`overflow != visible`、`filter` 都会把元素的 `transform-style` 强制成 flat。** 这是 3D 失效最常见的原因。此处 3D 一律下沉到 `.hero-perspective` 以内：`.scene` 上的 `opacity`（日夜淡出）与 `overflow: hidden`（裁切）只作用于投影之后的平面结果，互不干扰——因此页面骨架无需为 3D 改动。
2. **`filter: blur()` 会扁平化。** 远景竹竿原以 `blur(1.5px)` 失焦，已改为「降低墨色浓度 + `translateZ` 推远」；后者的近大远小本就是真 3D 的属性，比模糊更准。斑驳光斑与光柱仍需 blur，故不进 3D 层，作为平面元素另置。
3. **`translateZ` 会让内容按透视比例整体缩放。** `deep` 层在 1400px 透视、Z=−300 下缩到约 82%，且以 `perspective-origin` 为中心收缩。月轮的 `right` 已按此补偿（12% → 7%）；**任何放进深层或远层的全幅元素都会变成一块 82% 宽的居中矩形**——这正是星河最初露馅成「方板」的直接原因（见下条）。
4. **斜向渐变轴会在矩形对角留下硬边。** 星河带的渐变轴是左下→右上，于是左上角与右下角恰好落在轴心、颜色最浓，沿着 SVG 的直角露出两条直边。修法是给整层加径向 `mask-image` 让四边一起淡出——凡「斜向铺满」的渐变都要检查这一点。

### 大气层

- **纸纹** —— 一张 140×140 的 SVG 噪声平铺（不是整屏 `feTurbulence`，后者每次栅格化都是整屏开销），日间 `mix-blend-mode: multiply` 0.04，夜间 `overlay` 0.05（暗底上 multiply 只会更黑）。
- **渐晕** —— `--theme-vignette` 径向压暗边缘，日间 `rgba(10,10,11,0.10)`、夜间 `rgba(0,0,0,0.55)`，把注意力收到画面中心。
- **上下缘淡出** —— 用 `--theme-app` 的渐变把 Hero 与页面底色缝起来，消除场景底部的硬边。

### 标题

- **标题** —— `clamp(2.5rem, 6.5vw, 6rem)` 的衬线大字，上限 96px、1440px 下约 94px/字。字号刻意收在 6rem 以内：再大就会压过整屏、把 3D 场景挤成背景板，标题也就不再是「一屏之内的主角」而是「整屏唯一的内容」。
- **笔锋** —— 一道两端尖的短横（`hero-brush`）替代普通分隔线，`scaleX` 自左向右写就。

> **已移除：标题旁的朱红落款印**（曾为方章 + 单个汉字）。移除的两个理由都成立：印章紧贴标题会被读成落款，而落款按惯例是署姓的，容易让人误以为那个字是作者姓氏；方形朱印配单字的组合本身也偏传统、显旧。标题下方的朱红笔锋已承担了点缀色的作用，删后画面反而更干净。

### 入场时序

用**显式 delay 编排**而非嵌套 variant stagger——时序一眼可读，不必在脑子里推演变体传播。常量集中在 `HeroSection.tsx` 的 `T` 对象，改那里就能整体调节开幕节奏：

| 步骤 | 时间 | 动效 |
|------|------|------|
| 逐字落墨 | 0.15s 起，间隔 0.17s | 每字 `blur(18px) → 0` + 上移 34px，1.25s |
| 笔锋自绘 | 1.20s | `scaleX 0 → 1`，1.05s，origin 在左 |
| 标语 | 1.42s | 淡入 + 上移 14px |
| 入口 | 1.62s | 淡入 + 上移 16px |
| 滚动提示 | 2.00s | 淡入 |

### 入口按钮

`.hero-cta` 主次两级：`--primary` 为朱红描边、悬停时填充自左向右扫入并反白；`--ghost` 为中性描边、悬停转入朱红。字体走 `font-serif` 配 0.14em 字距（尾部字距用负 `margin-right` 抵掉以保证视觉居中），`min-height: 2.875rem` 满足触控目标。

### 动效清单

- **视角跟随** —— 指针位置映射为空间的 `rotateX`（±2.4°）/ `rotateY`（±3.6°），低刚度高阻尼弹簧（`stiffness 55 / damping 20 / mass 0.7`）。监听挂在 `window` 而非 section，指针移到页头上方时场景仍继续跟随。
- **滚动视差** —— 离场时各层以不同速度上移（`far −55 / mid −110 / near −180 / deep −30`px）。
- **循环氛围** —— 竹竿摇曳 9–14.5s、光斑漂移 18–23s、浮尘上浮 24–37s、云气漂移 34–41s、光柱呼吸 17–29s、星河所属星野明灭 5–7s、月光光环呼吸 7–9.5s、萤火游移与明灭 13–20s、竹叶旋落 19–29s。
- **重播机制** —— 点击 Header 品牌名触发 `replay-hero` 事件，以 `Fragment key` 重挂载内容，整段入场重播。

### 降级（四路，每条都需截图验证不空白）

1. **无 JS / 挂载前** —— 渲染普通 div，显隐交给 CSS 的 `[data-theme]` 规则；3D 由 CSS 的静态 `translateZ` 承担，仍有纵深，只是没有视角与视差。首帧不序列化任何内联 `opacity`，避免覆盖暗色规则导致暗色用户看到「白天竹林」闪一下。
2. **触屏 / 无精确指针** —— `(hover: hover) and (pointer: fine)` 不匹配时不启用视角跟随。滚动视差保留（它是滚动驱动，不依赖指针）。
3. **`prefers-reduced-motion`** —— 循环动画在 `hero-scene.css` 末尾统一关闭；星尘、萤火定格在静态可见帧，落叶直接隐藏（静止的落叶没有意义），滚动提示的线停在可见位置；入场编排整体跳过，内容直出。
4. **不支持 `preserve-3d`** —— 由 `@supports not (transform-style: preserve-3d)` 兜底，退回平面分层叠加。

### 绘制

竹竿与竹叶为 SVG `path`（竹竿 viewBox `0 0 26 1000` 配合 `preserveAspectRatio="none"` 拉伸，竹叶 viewBox `0 0 100 22`）。**不用圆角矩形 div 代替**：`border-radius` 拼不出竹叶两头尖的披针形，放大后像碎片。渐变集中在 `HeroDefs` 里一次定义（SVG sprite 手法），各处按 id 引用。

竹节环是竹竿最直接的辨识特征，每根竿按 `NODE_Y` 均布 6 道略下凹的弧，并在节下补一道白色高光（夜间换成月色），避免节环只是一条黑线。

星河里的 170 个星点由**固定种子的 mulberry32** 在模块作用域算好：渲染期调 `Math.random()` 会让 SSR 与客户端产出不一致，触发 hydration 警告。星点 y 坐标收在 viewBox 的 0–400 内，越界的会被 SVG 裁掉。

### 移动端

≤640px 时隐去中景竿——它是「在画面内收头挂叶」的那一根，叶簇按几何计算正好落在标语高度（竿 `bottom:-4%` + `height:58%` → 顶端在画面 46%，叶簇 48%–55%；标语在 53%）。桌面上它在 x≈144px、正文自 336px 起所以不冲突，窄屏则直接压上去。同时远山压低到 34%、云气与光柱减弱。
