# 环境变量示例
#
# 用法：复制为 .env.local 后填写。`.env*` 已被 .gitignore 忽略，
# 但 .env.example 本身会被提交（靠 .gitignore 里的 !.env.example 例外）。
#
# 全部变量都是**可选的** —— 一个都不填也能 `npm run build` 成功。
# 缺省时会退化为：域名用占位地址、无评论、无天气数据。详见每一项的说明。

# ─────────────────────────────────────────────────────────────
# 站点地址（上线必填）
# ─────────────────────────────────────────────────────────────
# 站点根 URL，**不带**结尾斜杠。用于 canonical、sitemap、RSS、分享图与 JSON-LD。
#
# 不设置也能构建，但页面里的永久链接会全部指向 https://example.com ——
# 搜索引擎会把 example.com 当作你的站点，属于静默的 SEO 故障。
# 构建期会就此打印一条警告提醒。
#
# 两种部署情形的取值：
#   用户站点 / 自有域名  https://your-name.github.io
#   项目页（子路径）      https://your-name.github.io/your-repo
NEXT_PUBLIC_SITE_URL=https://example.com

# ─────────────────────────────────────────────────────────────
# 子路径前缀（仅项目页需要）
# ─────────────────────────────────────────────────────────────
# 只在站点部署于子路径下时设置，值不带协议与域名。
#
# GitHub Pages 的**项目页**（https://user.github.io/repo/）必须设置，
# 否则 CSS/JS 会去根路径加载，整站白屏。
#
# 设置时必须与上面的 NEXT_PUBLIC_SITE_URL 保持一致（后者要含同一段子路径）：
#   NEXT_PUBLIC_SITE_URL=https://your-name.github.io/your-repo
#   NEXT_PUBLIC_BASE_PATH=/your-repo
NEXT_PUBLIC_BASE_PATH=

# ─────────────────────────────────────────────────────────────
# 评论（可选）
# ─────────────────────────────────────────────────────────────
# Waline 后端地址。不设置则留言板与文章评论区显示「评论系统未配置」。
# 自建方式见 README 的「可选依赖」一节。
#
# 设置后，scripts/keepalive-waline.mjs 会随构建一起 ping 该后端，
# 避免免费层数据库因长期无活动被暂停（约 7 天）。
NEXT_PUBLIC_WALINE_SERVER_URL=

# ─────────────────────────────────────────────────────────────
# 天气预警（可选，仅 /radar/weather 页用到）
# ─────────────────────────────────────────────────────────────
# 和风天气（https://dev.qweather.com/）的 API Key 与专属 API Host。
#
# 两个都要填才会抓取：和风为每个账号分配独立 Host（形如
# xxxxxx.re.qweatherapi.com），**无法复用他人的**。
#
# 不填时 scripts/fetch-weather-alerts.mjs 跳过抓取并以 0 退出，
# 不阻断构建；页面会渲染 data/radar/weather-alerts.json 里的既有数据
# （如有），或显示空态。
QWEATHER_KEY=
QWEATHER_HOST=

# ─────────────────────────────────────────────────────────────
# 说明：AI 模型排行榜（/radar/lmarena）
# ─────────────────────────────────────────────────────────────
# 无需环境变量 —— 该数据由 scripts/fetch-lmarena.mjs 从公开页面抓取。
# 抓取失败时会回退到仓库内已提交的 data/radar/lmarena.json，不阻断构建。
